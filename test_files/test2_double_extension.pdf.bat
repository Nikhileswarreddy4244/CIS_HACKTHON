This file is named "invoice.pdf.bat" - a double extension trick.
It pretends to be a PDF but hides a .bat extension inside.
The scanner should block this because ".bat" is dangerous even though the visible extension looks like ".pdf".
